You are evaluating multiple job postings for one candidate. Use the structured requirements below as the primary scoring rubric. Return ONLY one JSON object: {{"scores": [ ... ]}} with one score object per job, in the same order as below (job 1 first, etc.). Each object must match the required schema fields.

{scoring_input}

Title prefilter keywords (jobs already matched at least one): {target_keywords}

{jobs_block}

Scoring rules (apply to each job separately, strictly against structured requirements):

role_relevance (0–100): PRIMARY role family → 85–100; SECONDARY → 60–84; partial/adjacent → 40–59; AVOID or generic SWE → 0–39.

mission_alignment (0–100): excited-about sectors → 85–100; neutral → 50–84; weak → 21–49; deal-breaker industry → 0–20.

candidate_fit (0–100): seniority + skills + background + organization fit; penalize too_junior/too_senior and avoid/weak stack requirements.

Fit checks — decide each boolean strictly from the full posting. Candidate: EU citizen, based in the EU (CET), fully remote only, mid-level:
- remote_ok: true only for fully-remote roles; any required office days or relocation → false. Plain "Remote"/unspecified → true.
- eu_hire_ok: false when hiring is restricted to a non-EU region (e.g. "Remote — US only", "United States (Remote)", US city with no remote option, US work authorization required). EU/Europe hire region, global/work-from-anywhere, or nothing stated → true.
- timezone_ok: false when US business hours or substantial US-timezone overlap is required (EST/PST hours, "overlap with US team"). European/flexible/unstated hours → true.
- seniority_ok: false when the role is clearly above mid-level (senior/staff/principal/lead/head/management or far more required years) or below it (internship/graduate).
- fit_reasons: up to 3 short strings explaining any false boolean (empty array when all true).

Also populate: extracted_salary, top_requirements (≤5), risks_or_gaps (≤5), one_line_summary.

Be conservative on scores when information is missing — but for fit-check booleans, missing information means "no restriction stated".
